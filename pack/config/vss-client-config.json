{
  "_help": {
    "configVersion": "配置结构版本，升级时自动迁移；当前版本 v0.2.16，请勿手动修改。",
    "receiveServerLods": "是否接收服务端发送的 Voxy LOD；默认 true。",
    "lodDistanceChunks": "客户端请求 LOD 的半径，单位区块；默认 0；范围 0-8192，0 表示自动取服务端上限与 Voxy 设置中的较小值。",
    "desiredBandwidthKbps": "客户端期望的 LOD 下载带宽上限，单位 Kbps；默认 0；范围 0-100000（最高 100 Mbps），0 表示不额外限速，仍受服务端上限控制。",
    "offThreadSectionProcessing": "是否在后台线程处理收到的 LOD 区块以减少主线程卡顿；默认 true。",
    "enableXaeroMapBridge": "是否将服务端远景写入 Xaero 世界地图；默认 true。可用 /vssclient xaero disable 临时关闭。",
    "performanceTier": "预测性能档位 low/medium/high；默认 medium。low 使用四分之一核心并保留 45 FPS 保险丝，medium 使用一半核心并保留 30 FPS 保险丝，high 沿用旧版全核心配置且不自动降载。距离、细节等画质参数与档位无关。",
    "enablePrediction": "是否根据服务端同步的种子和世界生成元数据在远处生成预测地形；默认 true。",
    "predictionDistanceBlocks": "预测远景范围，单位方块；默认 4096；范围 1024-65536。它独立于 VSS 的 lodDistanceChunks。",
    "predictionDetail": "预测地形细节等级 low/normal/high/extreme；影响屏幕像素阈值。",
    "predictionFineDistanceBlocks": "普通精细地形距离，单位方块；默认 512，范围 256-4096，独立于预测远景距离；高空按实际距离降低精度，望远镜可突破此距离。",
    "predictionBackgroundWorkers": "视野外地形的后台任务槽；默认 2，范围 1-4。近处和当前视野优先，已加载的远景仍保留。",
    "predictionRefinementWorkers": "中等覆盖完成后的普通精修任务槽，包含视野内地形和植被；默认 0 表示跟随性能档位（low 3 / medium 6 / high 为一半逻辑线程），也可指定 1-32 手动覆盖。缺失覆盖、望远镜目标和脏列修复优先处理。",
    "predictionTrees": "是否生成近处及望远镜目标区域的原版树木、草等地表植被；默认 true。",
    "predictionStructures": "是否生成近处及望远镜区域的原版地表结构；默认 true。",
    "predictionSurfaceDistanceBlocks": "在已知近处地形外额外细化地表的范围，单位方块；默认 768，范围 128-2048；随真实地形覆盖边界向外延伸，远处只由望远镜触发。",
    "rememberTerrain": "是否在客户端压缩保存预测地形、植被及地表建筑，重访时读取并允许释放闲置网格；默认 true。",
    "predictionSupersample": "是否在最近两个 LOD 层级使用超采样；默认 false。",
    "predictionFog": "是否使用独立的远景雾；默认 true。",
    "debugLogging": "是否输出客户端 VSS 调试日志；默认 false。"
  },
  "configVersion": "v0.2.16",
  "receiveServerLods": true,
  "lodDistanceChunks": 0,
  "desiredBandwidthKbps": 0,
  "offThreadSectionProcessing": true,
  "enableXaeroMapBridge": true,
  "performanceTier": "medium",
  "enablePrediction": true,
  "predictionDistanceBlocks": 4096,
  "predictionFineDistanceBlocks": 512,
  "predictionBackgroundWorkers": 2,
  "predictionRefinementWorkers": 0,
  "predictionDetail": "normal",
  "predictionTrees": true,
  "predictionSurfaceDistanceBlocks": 768,
  "predictionStructures": true,
  "rememberTerrain": true,
  "predictionSupersample": false,
  "predictionFog": true,
  "debugLogging": false
}