#Show every bundle entry in the tooltip and allow scrolling through all of them
expandBundleTooltip = true
#While holding a bundle in an inventory, drag over slots with left click to insert items or right click to remove items
bundleDragEnabled = true
